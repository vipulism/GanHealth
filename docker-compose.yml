version: "3.9"

services:
  user-service:
    build: .
    container_name: user-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped

  # db:
  #   image: mysql:8
  #   container_name: mysql-db
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: user_db
  #     MYSQL_USER: myuser
  #     MYSQL_PASSWORD: myuser0809
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

# volumes:
#   mysql_data: